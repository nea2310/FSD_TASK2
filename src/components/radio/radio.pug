mixin radio(options)
  if !options
    - options = {}
  -
  label(class="radiomark__wrapper")
    input(type="checkbox" checked="checked")
    span(class="radiomark")
  div(class="radiomark__label")= options.labeltext